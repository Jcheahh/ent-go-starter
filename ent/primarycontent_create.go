// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"entdemo/ent/contentblock"
	"entdemo/ent/primarycontent"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PrimaryContentCreate is the builder for creating a PrimaryContent entity.
type PrimaryContentCreate struct {
	config
	mutation *PrimaryContentMutation
	hooks    []Hook
}

// SetPlaceholder sets the "placeholder" field.
func (pcc *PrimaryContentCreate) SetPlaceholder(i int) *PrimaryContentCreate {
	pcc.mutation.SetPlaceholder(i)
	return pcc
}

// SetNillablePlaceholder sets the "placeholder" field if the given value is not nil.
func (pcc *PrimaryContentCreate) SetNillablePlaceholder(i *int) *PrimaryContentCreate {
	if i != nil {
		pcc.SetPlaceholder(*i)
	}
	return pcc
}

// AddContentBlockIDs adds the "contentBlock" edge to the ContentBlock entity by IDs.
func (pcc *PrimaryContentCreate) AddContentBlockIDs(ids ...int) *PrimaryContentCreate {
	pcc.mutation.AddContentBlockIDs(ids...)
	return pcc
}

// AddContentBlock adds the "contentBlock" edges to the ContentBlock entity.
func (pcc *PrimaryContentCreate) AddContentBlock(c ...*ContentBlock) *PrimaryContentCreate {
	ids := make([]int, len(c))
	for i := range c {
		ids[i] = c[i].ID
	}
	return pcc.AddContentBlockIDs(ids...)
}

// Mutation returns the PrimaryContentMutation object of the builder.
func (pcc *PrimaryContentCreate) Mutation() *PrimaryContentMutation {
	return pcc.mutation
}

// Save creates the PrimaryContent in the database.
func (pcc *PrimaryContentCreate) Save(ctx context.Context) (*PrimaryContent, error) {
	return withHooks[*PrimaryContent, PrimaryContentMutation](ctx, pcc.sqlSave, pcc.mutation, pcc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (pcc *PrimaryContentCreate) SaveX(ctx context.Context) *PrimaryContent {
	v, err := pcc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pcc *PrimaryContentCreate) Exec(ctx context.Context) error {
	_, err := pcc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pcc *PrimaryContentCreate) ExecX(ctx context.Context) {
	if err := pcc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pcc *PrimaryContentCreate) check() error {
	return nil
}

func (pcc *PrimaryContentCreate) sqlSave(ctx context.Context) (*PrimaryContent, error) {
	if err := pcc.check(); err != nil {
		return nil, err
	}
	_node, _spec := pcc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pcc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	pcc.mutation.id = &_node.ID
	pcc.mutation.done = true
	return _node, nil
}

func (pcc *PrimaryContentCreate) createSpec() (*PrimaryContent, *sqlgraph.CreateSpec) {
	var (
		_node = &PrimaryContent{config: pcc.config}
		_spec = sqlgraph.NewCreateSpec(primarycontent.Table, sqlgraph.NewFieldSpec(primarycontent.FieldID, field.TypeInt))
	)
	if value, ok := pcc.mutation.Placeholder(); ok {
		_spec.SetField(primarycontent.FieldPlaceholder, field.TypeInt, value)
		_node.Placeholder = value
	}
	if nodes := pcc.mutation.ContentBlockIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   primarycontent.ContentBlockTable,
			Columns: []string{primarycontent.ContentBlockColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(contentblock.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// PrimaryContentCreateBulk is the builder for creating many PrimaryContent entities in bulk.
type PrimaryContentCreateBulk struct {
	config
	builders []*PrimaryContentCreate
}

// Save creates the PrimaryContent entities in the database.
func (pccb *PrimaryContentCreateBulk) Save(ctx context.Context) ([]*PrimaryContent, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pccb.builders))
	nodes := make([]*PrimaryContent, len(pccb.builders))
	mutators := make([]Mutator, len(pccb.builders))
	for i := range pccb.builders {
		func(i int, root context.Context) {
			builder := pccb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PrimaryContentMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pccb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pccb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pccb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pccb *PrimaryContentCreateBulk) SaveX(ctx context.Context) []*PrimaryContent {
	v, err := pccb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pccb *PrimaryContentCreateBulk) Exec(ctx context.Context) error {
	_, err := pccb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pccb *PrimaryContentCreateBulk) ExecX(ctx context.Context) {
	if err := pccb.Exec(ctx); err != nil {
		panic(err)
	}
}
